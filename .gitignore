.DS_Store
node_modules/
.wrangler/
.dev.vars
*.local

# Cloudflare installer output (contains real tokens — never commit)
.cloudflare.local.env
.nanob.local.env
*.local.env
.work/

# Generated wrangler.toml (use .example files as templates)
workers/**/wrangler.toml

# Telegram Bot (contains real tokens — never commit)
bot/.env
bot/.venv/

# Web Panel (contains real tokens — never commit)
web/.env
web/.venv/
web/__pycache__/

__pycache__/
.nanobk-wizard-state.json

# Local owner smoke test files (contain real values — never commit)
.nanobk-local/
*.local-owner-plan.json
*.local-credential.env
