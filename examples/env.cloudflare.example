# NanoBK Proxy Suite — Cloudflare Admin Environment
#
# This file is generated by installer/install-cloudflare.sh.
# Copy to /root/.nanok-cf-admin.env on your VPS for key rotation.
#
# chmod 600 /root/.nanok-cf-admin.env
# Do NOT commit this file with real values.

# Admin token — must match the ADMIN_TOKEN secret on the nanok Worker
ADMIN_TOKEN="<REPLACE_WITH_YOUR_ADMIN_TOKEN>"

# Admin API endpoints — replace YOUR_WORKER_HOST with your actual Worker URL
ADMIN_CURRENT_URL="https://YOUR_WORKER_HOST/admin/current"
ADMIN_UPDATE_URL="https://YOUR_WORKER_HOST/admin/update"

# Subscription URL (for reference, not used by rotation script)
# SUB_URL="https://YOUR_WORKER_HOST/jb?token=YOUR_SUB_TOKEN"
